# 18-0728_18-update-exp14

## Material Passport

```text
artifact_type=experiment_result
experiment_id=18
status=training_configuration_recorded_results_pending
verification_status=ANALYZED_FROM_PROVIDED_LAUNCH_MATERIAL
recorded_at=2026-08-13
```

## 状态

```text
训练配置与启动材料已登记
正式训练完成状态：待补
最终 checkpoint：待补
正式 LIBERO 评测：待补
```

附件只包含启动脚本、资产同步逻辑和训练超参，没有训练结束日志、最终 loss、checkpoint
清单或 `summary.csv`。因此本记录不将 100k 目标步数当作实际完成步数。

## 实验身份

```text
JOB_NAME=18-libero_spatial_no_gen_fastwam_optim
display_name=18-0728_18-update-exp14
machine_or_platform=Huawei DI / moxing OBS（具体作业 ID 待补）
baseline=实验 10 FastWAM-aligned No-Gen Spatial
optimizer_reference=实验 14 FastWAM optimizer alignment
code_commit=f98d358d09f566f263461e1923e8f3a58faf74b8
code_tarball_sha256=0f918ff246c864f233adfe8b99d7c08f424294991a6365b96745991278206478
code_tarball_size=3.62 MB
launcher=scripts/train_native_libero_spatial_no_gen_fastwam_optim_align_100k.sh
```

代码 tarball：

```text
obs://yw-2030-gy/external/wx1512724/code/Ego-WAM-f98d358d09f566f263461e1923e8f3a58faf74b8.tar.gz
```

附件记录该 tarball 在 DI 容器内完成 SHA256 `MATCH` 校验。OBS 访问密钥和 SwanLab API key
不写入本实验仓库。

## 科学问题与对照

Exp18 复现 Exp10 的 LIBERO Spatial No-Gen FastWAM-aligned 数据、模型和 rollout contract，
并使用修复了学习率调度 bug 的代码，将 optimizer 严格对齐 FastWAM。核心问题是：正确推进
scheduler 后，cosine decay、warmup、minimum LR 和 weight decay 是否能改善闭环成功率。

这是组合设置对照，不把 horizon、stride、padding、normalization、optimizer 等变量的
独立贡献混为单变量结论。

## 相对 Exp10 / Exp14 的设置

Exp10 的数据、模型和 action contract 保持不变：

```text
suite=libero_spatial
No-Gen; Understanding + Action full train
views=image,wrist_image
frame_sampling_stride=1; action_horizon=32; action_exec_horizon=10
drop_incomplete_action_chunks=false
delta_action_dim_mask=true,true,true,true,true,true,false
context_attention_mode=isolated; hidden_size=1024; robot_state_dim=8; action_dim=7
action_flow=direct v-prediction; shift=5; target_v=noise-action
normalization=min/max; normalization_eps=1e-8
```

Exp18 optimizer 对齐 FastWAM：

```text
lr=1e-4
lr_scheduler_type=cosine_with_min_lr
warmup_ratio=0.05
min_lr_ratio=0.01
weight_decay=0.01
total_steps=100000
```

代码修复内容：

```text
build_accelerator: step_scheduler_with_optimizer=false
_step_optimizer_and_scheduler: 仅在 sync_gradients=true 且 optimizer 未跳过时推进 scheduler
max_keep_ckpt=0 正确传参，保留全部 checkpoint
```

这修复了 Exp14 中 scheduler 由于错误推进而导致 LR 曲线走快的问题，并保留全部 checkpoint。

## 训练超参

```text
GPUs=8; batch/GPU=32; global_batch=256; gradient_accumulation=1
optimizer=AdamW; betas=(0.9,0.95); gradient_checkpointing=false
dtype=bf16; SEED=42; num_workers=4
logging_steps=10; save_steps=5000; max_keep_ckpt=0
```

## 资产与路径

```text
OBS root=obs://yw-2030-gy/external/wx1512724
environment=${OBS root}/envs/fastwam_libero.tar.gz
base_model=${OBS root}/models/sensenova/SenseNova-U1-8B-MoT-SFT
dataset=${OBS root}/datasets/nvidia/LIBERO_LeRobot_v3/libero_spatial
local_output=/cache/wx1512724/RUN/18-libero_spatial_no_gen_fastwam_optim
uploaded_output=${OBS root}/training_runs/18-libero_spatial_no_gen_fastwam_optim/latest_run/
training_log=/cache/wx1512724/logs/ads_exp18_train.log
```

预期每 5000 steps 保存一个 checkpoint，共 20 个；实际文件数量待训练结束材料确认。

## 启动流程

```text
setup conda -> install/verify moxing -> SHA256 校验代码 tarball
-> 下载 fastwam_libero 环境、SenseNova-U1、LIBERO Spatial 数据
-> 验证 egowam_native/CUDA/flex_attention/av/transformers/accelerate
-> SwanLab login -> 8 卡训练 -> 上传 latest_run、日志和 swanlog
```

附件未提供最终 precheck 输出或训练进程退出码，因此这部分是启动设计，不是完整运行证明。

## 正式评测协议（待训练完成）

```text
suite=libero_spatial; MuJoCo=3.3.2
tasks=10; trials/task=50
action_horizon=32; action_exec_horizon/replan=10
num_inference_steps=10; num_wait_steps=30
normalization=min/max; stats=<run>/dataset_stats.json
context_attention_mode=isolated; disable_gen_branch=true
```

应明确记录 action-flow seed。主要对照为实验 5 `440/500=88.0%`、实验 10 step 90000
`454/500=90.8%`；FastWAM 约 98.2% 仅作外部参考。

## 当前结论边界与待补

目前只能确认 Exp18 的研究设计与可复现启动材料，不能确认训练是否跑完、LR 是否按预期衰减、
checkpoint 是否完整上传，也不能给出成功率结论。

待补：训练开始/结束时间与作业 ID；最终 step/loss/LR 曲线；checkpoint 列表和校验和；
`dataset_stats.json`；正式 50-trial/task `summary.csv`；与 Exp10 相同 seed 的配对诊断。
